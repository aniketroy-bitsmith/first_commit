import 'package:flutter/material.dart';
class TCoinsScreen extends StatelessWidget {
  const TCoinsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('T coins data')),
    );
  }
}
