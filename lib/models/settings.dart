import 'package:flutter/material.dart';

class Settings {
  const Settings({
    required this.title,
    required this.about,
    required this.icon,
  });

  final String title;
  final String about;
  final Icon icon;
}
